@using Ext.Net
@using Ext.Net.MVC
@{
    Layout = null;
}
@(Html.X()
    .Container()
    .Layout(LayoutType.Border)
        .ItemsFromPartial("PerformanceUI")
        //.Loader(Html.X().ComponentLoader()
        //            .Url(Url.Action("PersionUI"))
        //            .Mode(LoadMode.Component)
        //            .Params(new { containerId = JRawValue.From("this.id") })
        //            .LoadMask(mask =>
        //            {
        //                mask.ShowMask = true;
        //                mask.Msg = "加载中...";
        //            })
        //)
)