@{
    Layout = null;
}

@(
 Html.X().Container()
        .Loader(
            Html.X().ComponentLoader()
                .Url(Url.Action("Index"))
                .Mode(LoadMode.Frame)
                .LoadMask(mask => {
                    mask.ShowMask = true;
                    mask.Msg = "加载中...";
                })
        )
)