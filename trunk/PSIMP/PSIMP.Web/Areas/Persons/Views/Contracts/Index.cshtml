@{
    ViewBag.Title = "Index";
}
@(
 Html.X().GridPanelFor<PM_PersonContractInfo>()
        .Title(Html.MvcSiteMap().SiteMapPath("SiteMapPathHelperModel").ToString())
        .EmptyText("没有人员合同信息记录")
        .DockedItems(
               Html.X().SearchPanel()
                   .Items(
                       Html.X()
                           .TextField()
                           .FieldLabel("姓名")
                           .Width(150)
                           .LabelAlign(LabelAlign.Right)
                           .LabelWidth(35)
                   )
                   .Buttons(
                        Html.X().Button().Text("查询").Icon(Icon.Magnifier).Margins("0 0 0 10")
                   )
          )
        .TopBar(
            Html.X().Toolbar()
                .Items(
                    Html.X().Button().Text("新增人员合同信息").Icon(Icon.Add).Handler("person.contract.create")
                )
        )
        .BottomBar(Html.X().PagingToolbar())
)

